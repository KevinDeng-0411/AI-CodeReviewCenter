# Agent 阶段验收报告模板

> 本文件用于生成 `evidence/Sx/report.md`，供人阅读。阶段是否完成只由同目录 `manifest.json` 及其哈希引用的产物决定，规则见[阶段证据清单与解锁规则](../证据清单与解锁规则.md)。

---

## 1. 元信息

- 阶段：
- route profile（个人默认必须为 `personal-local-readonly`）：
- 实施日期：
- 实施者/模型：
- Git branch：
- 起始 commit：
- 实现 commit / 直接父提交：
- 验证 HEAD：
- 全部直接依赖 manifests 路径 / SHA-256 / validated commit：
- 运行环境：
- manifest 路径：`evidence/Sx/manifest.json`

## 2. 解锁依据

- C1/C2/C3/C4 manifest 验证结果：
- 当前卡全部直接依赖 manifests 验证结果：
- 用户在全部直接依赖证据形成后的明确授权原话：
- 任务引用与授权时间：
- 本阶段解锁状态（必须为 `IMPLEMENTATION_UNLOCKED:Sx`）：

## 3. 实施边界

- 本阶段目标：
- 实际修改：
- 明确未修改：
- 与计划偏差及原因：

## 4. 实施前基线与安全执行器

```text
命令：
结果：
既有失败：
一次性 PostgreSQL / Redis 目标：
安全保护结果：
```

## 5. 数据与接口变化

- Alembic revision：
- 新增/变更表：
- 新增/变更 API：
- 新增/变更环境变量：
- 兼容策略：

## 6. 自动测试证据

### Python

```text
命令：
结果：
```

### 前端

```text
命令：
结果：
```

### 集成/故障/安全测试

```text
命令：
结果：
```

## 7. 演示闭环

### 前置数据

```text
```

### 操作步骤

1.
2.
3.

### 实际输出

```text
```

### 持久化/事件/产物核验

```text
```

## 8. 故障与回退验证

- 故障注入：
- 临时 worktree / 一次性数据库：
- 回退方式（不得操作当前工作树或真实数据库）：
- 回退后验证结果：

## 9. 指标

| 指标 | 基线 | 本阶段 | 结论 |
|---|---:|---:|---|
| 成功率 |  |  |  |
| P95 延迟 |  |  |  |
| 平均模型回合 |  |  |  |
| 平均工具次数 |  |  |  |
| Token/成本 |  |  |  |
| Citation 正确率 |  |  |  |

## 10. manifest 产物索引

| check id | 产物相对路径 | SHA-256 | 结果 |
|---|---|---|---|
|  |  |  |  |

## 11. 已知限制

-

## 12. DoD 核对

- [ ] 阶段文档中的所有测试通过
- [ ] 演示可从空环境重复执行
- [ ] 异常与边界路径已验证
- [ ] 回退路径已验证
- [ ] 文档和 API schema 已同步
- [ ] 没有提前实现能力 DAG 中尚未授权的后继卡
- [ ] `validate_stage_evidence.py Sx` 通过
- [ ] `personal-local-readonly` 只证明本机单用户模式，未宣称远程安全
- [ ] S3 未选择时没有伪造 skipped/passed manifest
- [ ] durable、patch、shell、R2/R3、remote、MCP、多 Agent 路径不存在或硬关闭

## 13. 交接结论

- [ ] 可以请求用户评审并授权能力 DAG 中的下一张默认卡
- [ ] 不可以请求任何后继卡授权

原因：
