# 当前版本阶段验收报告模板

> 本文件是人类可读报告模板。完成 C1、C2 或 C3 时复制为 `evidence/Cx/report.md`，同时生成唯一机器入口 `evidence/Cx/manifest.json` 和其引用的 `artifacts/`。格式、哈希、Git 祖先、必需 checks 与解锁语义以[阶段证据清单与解锁规则](../证据清单与解锁规则.md)为准。
>
> Markdown 勾选框、报告文字或旧的单文件 Markdown evidence 均不能解锁阶段。失败尝试写入 `evidence/Cx/attempts/<run_id>/`，不得覆盖当前有效 manifest。

## 1. 元信息

- 阶段：
- route profile：`current-release`
- run_id：
- 日期（UTC）：
- 实施者/模型：
- baseline_commit：
- implementation_commit：
- implementation_parent：
- validated_head：
- 直接前置阶段 manifest 路径 / SHA-256 / validated commit（C1 为空）：
- manifest：`evidence/Cx/manifest.json`
- 被替代 manifest SHA-256（无则 null）：

## 2. 边界

- 本阶段目标：
- 实际修改：
- 明确未实现：
- 与计划偏差：
- 用户工作区原有修改及保护方式：

## 3. 一次性环境与 fail-closed 证据

- `run_tests_safe.py` stack_id：
- 临时 PostgreSQL host/port（脱敏）：
- 临时应用测试库：
- 临时 migration roundtrip 库：
- 临时 Redis 实例/非 0 DB：
- destructive-fixture target guard 结果：
- 拒绝 `ai_center`/`ai_center_py`/开发 Redis/伪造 identity 的测试产物：
- 测试前后开发数据指纹：
- 容器、网络、volume、数据库清理结果：

不得记录密码、token、完整连接串或宿主敏感绝对路径。任一目标校验或清理失败时，本阶段只能是 failed。

## 4. 实施前基线

```text
仓库根标识（仅仓库名/commit，不写宿主绝对路径）：
git status：
已知失败：
前端 lint/build：
通过安全执行器得到的后端基线：
```

禁止在报告中用裸 `pytest` 结果作为证据。后端命令必须经 `codeaware-py/scripts/run_tests_safe.py`。

## 5. 自动命令

manifest 的每条 required command 都必须在此对应，且包含 argv 数组、仓库相对 cwd、退出码、起止 UTC 时间、日志相对路径和 SHA-256：

| id | cwd | argv | exit | started/finished | artifact + SHA-256 |
|---|---|---|---:|---|---|
|  |  |  |  |  |  |

所有可复制命令从仓库根开始。阶段最终验证命令：

```bash
repo_root="$(git rev-parse --show-toplevel)"
cd "$repo_root"
(cd codeaware-py && uv run python scripts/validate_stage_evidence.py Cx)
```

## 6. 阶段 checks 聚合

每个 check 必须在 manifest 中使用稳定 id、`status: passed`，并引用至少一个带 SHA-256 的相对 artifact：

- C1：`C1-SAFE-HARNESS`、`C1-A`、`C1-B`、`C1-C`、`C1-D`、`C1-E`
- C2：`code-review`、`unit-test`、`ai-readme`、`chat`、`knowledge`、`memory`、`prompt`
- C3：`fresh-bootstrap`、`docs-contract`、`rollback`、`freeze-handoff`

本阶段实际 checks 与产物：

| check id | 正常/失败/边界覆盖 | artifact + SHA-256 |
|---|---|---|
|  |  |  |

## 7. 演示闭环

- 前置 fixture：
- 用户操作：
- 用户可见结果：
- API/OpenAPI 核验：
- PG/Redis/文件终态：
- 失败路径：
- browser E2E：
- live smoke（C2 必需一次；模型、日期、耗时、成本、脱敏输出）：

C2 必须逐域填写 API 和浏览器结果；不能用三条核心路径代替七个已有 UI 功能域。

## 8. 契约核验

- OpenAPI artifact + SHA-256：
- Alembic heads/current artifact + SHA-256：
- 前端 client/types：
- README 示例：
- typed SSE schema/trace：
- 三者是否一致：

## 9. 回退

- detached 临时 worktree commit：
- 临时 worktree 已与用户当前 worktree 隔离：
- 回退专用一次性 PG/Redis stack_id：
- migration upgrade/downgrade：
- 备份/恢复演练：
- 回退测试命令、退出码：
- 用户 worktree 与开发数据前后未变化：
- worktree/stack 精确清理：
- rollback artifact + SHA-256：

禁止在用户当前 worktree 执行 `checkout/reset/clean`，禁止对开发、个人或生产数据库执行 downgrade 取证。

## 10. 限制与结果

- 已知限制：
- 未解释 skip/warning：
- 需要用户提供的外部条件：
- manifest `result`：

## 11. 人类可读 DoD

- [ ] 阶段卡所有必需测试、演示和回退通过
- [ ] 所有后端测试经 `run_tests_safe.py`，目标校验 fail closed
- [ ] 成功、失败和边界路径均有哈希产物
- [ ] 文档、OpenAPI、前端与实现一致
- [ ] 未提前实现 Agent 能力
- [ ] 用户已有修改、开发 PG/Redis 和 volume 未被覆盖
- [ ] manifest 只引用仓库内相对路径，且所有 SHA-256 有效
- [ ] `validate_stage_evidence.py Cx` 返回 0

## 12. 交接

- [ ] C1/C2 manifest 验证通过，允许进入下一当前版本阶段
- [ ] C3 manifest 验证通过，只解锁 C4；C4 manifest 验证通过后才形成
  `REVIEW_UNLOCKED`，允许请求 Agent 路线评审
- [ ] manifest 未通过，不允许进入下一阶段

理由：

C3 额外填写：

```text
当前版本是否完成：是 / 否
是否允许“评审” Agent 路线：是 / 否
是否授权“实施” Agent 第一阶段：否（实施授权必须在 C3 验证通过后由用户另行给出）
默认评审档案：personal-local-readonly
```
